# DeepNotes backend — top-level dependencies (Python 3.12).
# Install:  uv venv --python 3.12 .venv && source .venv/bin/activate && uv pip install -r requirements.txt

# Web
fastapi
uvicorn[standard]
python-multipart
pydantic-settings

# Persistence
sqlalchemy

# Parsing + RAG (the OSS heavy-lifting)
docling
rapidocr-onnxruntime   # OCR fallback for scanned/image-only PDFs (portable, CPU/Linux)
llama-index-core
llama-index-llms-google-genai
llama-index-embeddings-google-genai

# Vector store
chromadb
llama-index-vector-stores-chroma

# Spreadsheet reasoning (XLSX differentiator)
duckdb
pandas
openpyxl

# Misc
python-dotenv

# Local sovereignty mode (LLM_PROVIDER=ollama) — small clients, no cloud calls:
llama-index-llms-ollama
llama-index-embeddings-ollama

# Phase 0 spike helper (test PDF generation):
# fpdf2
